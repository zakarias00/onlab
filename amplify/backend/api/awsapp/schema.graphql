type User @model @auth(rules: [{allow: owner, operations: [create, delete, update]}]) {
  id: ID!
  name: String
  first_name: String
  second_name: String
  birthday: String
  school: String
  email: AWSEmail
}
 